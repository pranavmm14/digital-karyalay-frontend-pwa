// List of routes (paths) that are not yet implemented and should show the ComingSoon page.
// Add paths exactly as used in your route definitions, e.g. '/reports' or '/management'.
// Keep empty in production or when pages are implemented.
const unfinishedRoutes: string[] = [
  '/reports',
  '/management'
];

export default unfinishedRoutes;
